# outputs/models/

Stores saved machine learning model files.

Expected contents:
- `best_flood_model.pkl` — the selected best classifier (saved with joblib)
- Optionally: individual model files for RF, XGBoost, SVM for reference
